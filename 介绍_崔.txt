当前文件下的readme是整个开源项目的介绍，requirements.txt是需要的环境。
上次吃饭的时候我说的我这边写了两个：
     第一个是所有的算法线性的预算，计算完第一个模块计算第二个，这个代码是demo_my.py 直接运行他就可以了，
最后保存的是json文件。json里包括了人体的框和关键点还有当前的状态。
     第二个是每个算法开一个队列，这部分代码是：webcam_demo_my.py，都是直接运行就可以了。